<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="14dp"
    android:paddingTop="10dp"
    android:paddingRight="14dp"
    android:paddingBottom="10dp"
    tools:background="#1a8cf7a0">

    <ImageView
        android:id="@+id/habit_thumb_iv"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        tools:src="@tools:sample/avatars"/>

    <TextView
        android:id="@+id/habit_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:layout_marginLeft="8dp"
        android:layout_toRightOf="@id/habit_thumb_iv"
        android:gravity="center_vertical"
        android:minHeight="24dp"
        android:textColor="#333333"
        android:textSize="13sp"
        tools:text="减肥1234"/>

    <com.louise.base.view.flexbox.UIFlexBoxView
        android:id="@+id/habit_flex_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/habit_thumb_iv"
        android:layout_marginTop="14dp"
        app:flexDirection="row"
        app:justifyContent="space_between"
        tools:background="#ff0"
        tools:layout_height="100dp"/>
</RelativeLayout>